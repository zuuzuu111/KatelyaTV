# =============================================================================
# Vercel + Upstash 一键部署环境变量配置示例
# =============================================================================
# 
# 此文件展示了使用 Vercel Upstash 集成时需要的环境变量配置
# 使用方法：
# 1. 在 Vercel 部署项目后，添加 Upstash 集成
# 2. 在 Vercel 项目的 Environment Variables 中添加以下变量
# 3. 重新部署项目
#
# 注意：UPSTASH_REDIS_REST_URL 和 UPSTASH_REDIS_REST_TOKEN 
# 会由 Vercel Upstash 集成自动注入，无需手动设置
# =============================================================================

# 存储后端类型（必须设置为 upstash）
NEXT_PUBLIC_STORAGE_TYPE=upstash

# 管理员账号配置（必需）
USERNAME=admin
PASSWORD=your_admin_password

# 允许新用户注册（可选）
NEXT_PUBLIC_ENABLE_REGISTER=true

# 以下变量由 Vercel Upstash 集成自动注入，无需手动配置：
# UPSTASH_REDIS_REST_URL=https://your-region.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_auto_generated_token

# =============================================================================
# 站点配置（可选）
# =============================================================================

# 站点标题
NEXT_PUBLIC_SITE_NAME=KatelyaTV

# 站点描述
NEXT_PUBLIC_SITE_DESCRIPTION=免费在线影视平台

# 默认豆瓣榜单 ID（可选，用于首页展示）
NEXT_PUBLIC_DEFAULT_DOUBAN_ID=49417645
